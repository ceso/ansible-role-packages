---
galaxy_info:
  role_name: packages
  author: ceso
  description: Install packages via OS native package manager, Linux Homebrew, Python uv, AppImages and plain binaries
  license: MIT

  min_ansible_version: "2.15"

  platforms:
    - name: Debian
      versions:
        - bookworm
    - name: Fedora
      versions:
        - "43"

  galaxy_tags:
    - linux
    - packages
    - homebrew
    - bootstrap
    - appimage
    - binaries

  # Role version (tracked via git tags in standalone repo)
  # version: 1.0.0

dependencies: []
